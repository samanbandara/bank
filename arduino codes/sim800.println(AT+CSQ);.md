sim800.println("AT+CSQ");

  int rssi = 0;

  String resp = readClean();

  if (resp.indexOf("+CSQ:") != -1) {

    int csqIndex = resp.indexOf("+CSQ:");

    if (csqIndex != -1) {

      int colon = resp.indexOf(':', csqIndex);

      int comma = resp.indexOf(',', colon);

      if (colon != -1 \&\& comma != -1) {

        int rssiVal = resp.substring(colon + 1, comma).toInt();

        rssi = -113 + (rssiVal \* 2);

      }

    }

  }

  if (0 > rssi) {

    if (rssi >= -105) oled.fillRect(119, 8, 1, 1, SSD1306\_WHITE);  // bar1

    if (rssi >= -95) oled.fillRect(121, 6, 1, 2, SSD1306\_WHITE);   // bar2

    if (rssi >= -85) oled.fillRect(123, 4, 1, 4, SSD1306\_WHITE);   // bar3

    if (rssi >= -75) oled.fillRect(125, 2, 1, 6, SSD1306\_WHITE);   // bar4

    if (rssi >= -65) oled.fillRect(127, 0, 1, 8, SSD1306\_WHITE);   // bar5

  }

  if (rssi <= -105) oled.fillRect(119, 8, 1, 1, SSD1306\_BLACK);  // bar1

  if (rssi <= -95) oled.fillRect(121, 6, 1, 2, SSD1306\_BLACK);   // bar2

  if (rssi <= -85) oled.fillRect(123, 4, 1, 4, SSD1306\_BLACK);   // bar3

  if (rssi <= -75) oled.fillRect(125, 2, 1, 6, SSD1306\_BLACK);   // bar4

  if (rssi <= -65) oled.fillRect(127, 0, 1, 8, SSD1306\_BLACK);   // bar5



 

  Serial.print(rssi);

  Serial.print("  ");





&nbsp; oled.setTextSize(1);

  oled.setTextColor(SSD1306\_WHITE);

  //to show tower icon

  oled.fillRect(114, 0, 7, 1, SSD1306\_WHITE);

  oled.fillRect(115, 1, 5, 1, SSD1306\_WHITE);

  oled.fillRect(116, 2, 3, 1, SSD1306\_WHITE);

  oled.fillRect(117, 3, 1, 6, SSD1306\_WHITE);

  //to show dots

  oled.fillRect(119, 8, 1, 1, SSD1306\_WHITE);

  oled.fillRect(121, 8, 1, 1, SSD1306\_WHITE);

  oled.fillRect(123, 8, 1, 1, SSD1306\_WHITE);

  oled.fillRect(125, 8, 1, 1, SSD1306\_WHITE);

  oled.fillRect(127, 8, 1, 1, SSD1306\_WHITE);

  oled.display();

